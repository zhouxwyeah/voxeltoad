apiVersion: v1
kind: Secret
metadata:
  name: voxeltoad-gateway-secret
  labels:
    app.kubernetes.io/name: voxeltoad
    app.kubernetes.io/instance: {{ .Release.Name }}
type: Opaque
# NOTE: create the actual secret out-of-band, e.g.:
#   kubectl create secret generic voxeltoad-gateway-secret \
#     --from-literal=internal-token=$(openssl rand -hex 32)
# The data is intentionally empty here — Helm should not materialize secrets.
